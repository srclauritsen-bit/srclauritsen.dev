# srclauritsen.dev

A minimal resume website scaffold.

Preview locally

Open `index.html` in your browser, or run a simple static server:

```bash
# Python 3
python3 -m http.server 8000

# then open http://localhost:8000
```

Files added:

- [index.html](index.html)
- [styles.css](styles.css)
- [script.js](script.js)

Next steps: customize content in [index.html](index.html), replace contact and project links, and optionally deploy with GitHub Pages.

## Deploy with GitHub Pages

1. Push this repository to GitHub.
2. In GitHub, open repository `Settings` -> `Pages`.
3. Under `Build and deployment`, choose `Deploy from a branch`.
4. Select branch `main` and folder `/ (root)`, then save.
5. After deployment, your URL will be:

```
https://<your-github-username>.github.io/srclauritsen.dev/
```

If your repository name is different, replace `srclauritsen.dev` with your repo name.